<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.financialcalculator">
    <uses-permission android:name="android.permission.CALL_PHONE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/fc1"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".InflectionActivity"></activity>
        <activity android:name=".CIinterestActivity" />
        <activity android:name=".EmicompActivity" />
        <activity android:name=".FdActivity" />
        <activity android:name=".GstActivity" />
        <activity android:name=".RdActivity" />
        <activity android:name=".PpfActivity" />
        <activity android:name=".SipActivity" />
        <activity android:name=".EmiActivity" />
        <activity android:name=".SecondActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>